using System;

namespace Bonus3
{
    class Program
    {
        static void Main()
        {
            //generate random number
            //get 1-100 int from user 
            //more than ten too high "Way too high"
            //more than ten too low "Way too low"
            //less than ten too high "Too high"
            //less than ten too low "Too low"
            //win message should be different based on how many guesses it takes them
            //only accept numbers 1-100

            //y/n continue
            //if invalid continue data, prompt again
            //separate methods
        }
    }
}
